# Shadow Pipeline Execution Log — Manual Run

**Server:** t-no1fkmvft-db  
**Started:** 2026-03-20 14:04:20 (Friday)  
**Completed:** 2026-03-21 07:48:38 (Saturday)  
**Total Duration:** 1,064.3 minutes (~17.7 hours)  
**Triggered by:** Manual execution (PID 1508, user `t1_srv_fkmvft_db`)  
**Result:** All steps OK

## Pipeline Summary

| Step | Start | End | Duration | Status |
|---|---|---|---|---|
| Step 1 (PRD Restore + Shadow Create) | 14:04 Fri | 16:10 Fri | 126.3 min | OK |
| Step 2 (Copy Data) | 16:10 Fri | 03:48 Sat | 697.4 min | OK |
| Step 3 (Verify Schema Objects) | 03:48 Sat | 03:48 Sat | 0.7 min | OK |
| Step 5 (Verify Row Counts) | 03:48 Sat | 04:04 Sat | 15.7 min | OK |
| Step 4 (Move to Original Instance) | 04:04 Sat | 07:48 Sat | 224.1 min | OK |
| Final Verify | 07:48 Sat | — | — | PASS (1 row in dbm.AH_ORDREHODE) |

## All Errors

### 1. SQL0554N — Self-grant (Step 1 / Db2-CreateInitialDatabases)

```
2026-03-20 16:06:44 | PID 9896 | ERROR
SQL0554N  En autorisasjons-ID kan ikke gi en rettighet eller autorisasjon til seg selv.
```

**Severity:** Benign — expected during grants phase. The service account `t1_srv_fkmvft_db` (SYSADM) tries to grant privileges to itself, which DB2 rejects. Does not affect pipeline outcome.

### 2. LINJE MELDINGER TIL db2_adminotm.bnd (Step 2 / DDL bind)

```
2026-03-20 16:10:56 | PID 8400 | ERROR
LINJE   MELDINGER TIL db2_adminotm.bnd
```

**Severity:** Benign — part of `db2look` DDL application bind messages. Norwegian DB2 output formatting causes the line to be classified as ERROR by `Test-OutputForErrors`, but it's informational.

### 3. SQL0204N — DBM.TRU.AXIA_ORDRE not defined (Step 5 / Verify Row Counts)

```
2026-03-21 03:54:42 | PID 6128 | ERROR
SQL0204N  "DBM.TRU.AXIA_ORDRE" er ikke et definert navn.  SQLSTATE=42704
```

```
2026-03-21 04:01:07 | PID 6128 | ERROR
SQL0204N  "DBM.TRU.AXIA_ORDRE" er ikke et definert navn.  SQLSTATE=42704
```

**Severity:** Minor — this is a nickname/alias referencing a remote table (`TRU.AXIA_ORDRE`) that doesn't exist as a local table. It appears in both source and target verification queries. Step 5 classified it as "missing" (not mismatched) and the pipeline continued.

### 4. SQL1493N — Already connected (Step 4 / Cleanup)

```
2026-03-21 06:03:46 | PID 11216 | ERROR
SQL1493N  Kommandoen mislyktes fordi applikasjonen allerede er koblet til en database.
```

```
2026-03-21 07:48:25 | PID 11216 | ERROR  
SQL1493N  Kommandoen mislyktes fordi applikasjonen allerede er koblet til en database.
```

**Severity:** Benign — occurs during cleanup when `db2 connect` is issued while a previous connection is still active. Harmless in script flow.

### 5. SQL1013N — Database alias not found (Step 4 / Post-move cleanup)

```
2026-03-21 06:03:49 | PID 11216 | ERROR
SQL1013N  Databasealiaset eller databasenavnet "FKMVFT" ble ikke funnet.
```

```
2026-03-21 07:48:26 | PID 11216 | ERROR
SQL1013N  Databasealiaset eller databasenavnet "FKMVFTSH" ble ikke funnet.
```

**Severity:** Benign — expected during Step 4's uncatalog/recatalog sequence. The database is dropped and recreated during the move, so queries against the old catalog entry naturally fail.

### 6. SQL1021N — Node not found (Step 4 / Post-move cleanup)

```
2026-03-21 07:48:27 | PID 11216 | ERROR
SQL1021N  Noden "SHINST" som du oppgav i kommandoen UNCATALOG NODE, ble ikke funnet.
```

**Severity:** Benign — the shadow node `SHINST` is uncataloged as part of cleanup. If already removed, this error appears. Does not affect outcome.

## All Warnings

### 1. Post-restore credentials (Step 1)

```
2026-03-20 14:04:40 | PID 10756 | WARN
Setting post restore credentials for database FKMVFT. DbUser: db2nt, DbPassword: ntdb2
```

**Severity:** Informational — standard credential setup after restore.

### 2. db2ubind.lst / db2clpnc.bnd bind messages (Step 2 / DDL)

```
2026-03-20 16:10:54 | PID 8400 | WARN
LINJE   MELDINGER TIL db2ubind.lst
LINJE   MELDINGER TIL db2clpnc.bnd
```

**Severity:** Benign — bind package output during DDL application.

### 3. SQL0598W — Existing index reused (Step 2 / DDL)

```
2026-03-20 16:20:24 | WARN | SQL0598W  Den eksisterende indeksen "DBM.TQ_LOGIN_I1" er brukt som indeks for primærnøkkelen eller unik nøkkel
2026-03-20 16:21:42 | WARN | SQL0598W  Den eksisterende indeksen "DBM.MITFACMINI_01" er brukt som indeks
```

**Severity:** Benign — DB2 reused existing indexes for primary/unique keys. This is normal behavior.

### 4. SQL0403W — Alias pointed to missing object (Step 2 / DDL)

```
2026-03-20 16:28:17 | WARN | SQL0403W  Aliases pointing to non-existent objects:
  DBM.DRBIDRSALG98, DBM.DRBIDRSALG98_SUM, DBM.DRBIDRSALG98_SUM1,
  DBM.DRBIDRSALG98_SUMF, DBM.DRBIDRSALG99K_SUM1,
  TV.FAKTHIST_0, TV.FAKTHIST_1, TV.FAKTHIST_2
```

**Severity:** Low — old aliases in the DDL referencing tables that no longer exist. Inherited from source database DDL. No functional impact.

### 5. SQL3107W — LOAD warning (Step 2 / db2move LOAD)

```
2026-03-20 20:51:31 | PID 8400 | WARN
SQL3107W  Det ble mottatt minst en advarselsmelding under LOAD-behandlingen.
```

**Severity:** Low — generic LOAD warning. Context shows it occurred during the Norwegian character individual export phase (around `DBM_TRANSPORTOER`, `MEH_CONTROL_VRANGLAAS`). No data was lost — all row counts matched in Step 5.

### 6. DBM.TRU.AXIA_ORDRE missing (Step 5 / Verify)

```
2026-03-21 04:03:57 | PID 6128 | WARN
MISSING: DBM.TRU.AXIA_ORDRE | Source(N/A): -1 | Target(N/A): -1
Row count verify: 2974 match, 0 mismatch, 1 missing of 2975 tables.
```

**Severity:** Low — a nickname (not a real table) that doesn't exist in either source or target. Both report -1 rows. Not a data loss issue.

### 7. No federated access point (Step 4 cleanup + Final Verify)

```
2026-03-21 07:48:13 | PID 11216 (Step-4-MoveToOriginalInstance) | WARN
No federated access point configured for  - skipping federation
```

```
2026-03-21 07:48:32 | PID 1508 (Run-FullShadowPipeline / Final Verify) | WARN
No federated access point configured for  - skipping federation
```

**Severity:** Benign — after Step 4 restores FKMVFT back onto the DB2 instance, `Get-DefaultWorkObjectsCommon` rebuilds the work objects for all databases on the server. During this rebuild, it checks each database for a `FederatedDb` access point in `DatabasesV2.json`. The FKMVFT database has a federated entry (`XFKMVFT` on `DB2SH`), but the *restored* database being reconfigured here does not carry that property yet — the federation was only needed during the shadow phase and is not relevant to the final DB2 instance. The warning is emitted because the lookup returns an empty string, so it logs "skipping federation" and moves on. No federation is needed or expected on the restored database.

## Error/Warning Summary

| Category | Count | Severity | Impact |
|---|---|---|---|
| SQL0554N (self-grant) | 1 | Benign | None |
| DDL bind messages | 2 | Benign | None |
| SQL0204N (nickname not found) | 2 | Minor | 1 table classified as "missing" in Step 5 |
| SQL1493N (already connected) | 2 | Benign | None |
| SQL1013N (alias not found) | 2 | Benign | Expected during move |
| SQL1021N (node not found) | 1 | Benign | Expected during cleanup |
| SQL0598W (index reused) | 2 | Benign | None |
| SQL0403W (alias target missing) | 6 | Low | Old aliases, no functional impact |
| SQL3107W (LOAD warning) | 1 | Low | No data loss |
| TRU.AXIA_ORDRE missing | 1 | Low | Nickname, not real data |
| Federation skip | 2 | Benign | None |
| Credential WARN | 1 | Info | Standard behavior |

**Conclusion:** No blocking errors. All errors and warnings are either expected DB2 behavior (self-grant, connection state, catalog cleanup) or low-severity cosmetic issues (old aliases, nickname references). The pipeline completed with 2,974 of 2,975 tables matched (1 missing = nickname `DBM.TRU.AXIA_ORDRE`, not a real table).

## Comparison to Previous Runs

| Metric | This run (Mar 20) | autocur run (Mar 18) | First run (Mar 16) |
|---|---|---|---|
| Total time | 1,064 min | 1,115 min | 1,170 min |
| Step 2 (Copy Data) | 697 min | 679 min | 720 min |
| Step 4 (Move) | 224 min | 293 min | 224 min |
| Bugfixes needed | 0 | 0 | 5+ |
| Row mismatches | 0 | 0 | 25 |
| Missing tables | 1 (nickname) | 0 | 25 (Norwegian chars) |
