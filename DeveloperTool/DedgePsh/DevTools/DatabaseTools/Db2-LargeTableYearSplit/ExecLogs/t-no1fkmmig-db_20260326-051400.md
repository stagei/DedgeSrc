# Db2-LargeTableYearSplit — Execution Log

**Server:** t-no1fkmmig-db  
**Database:** FKMMIG (Db2 12.1 LUW)  
**Started:** 2026-03-26 05:14  
**Completed:** 2026-03-26 08:15  
**Total duration:** ~15 min (pipeline runtime 08:01–08:15)  
**Method:** RenameAndReload  
**RestoreFromPrd:** No  
**Tables:**
1. DBM.VISMA_TRANSER (TimestampColumn: TIDSPUNKT_VISMA)
2. DBM.D365_BUNTER (TimestampColumn: T_TPKT_VISMA)

**Skill:** large-table-split-autocur  
**Code changes:** RENAME INDEX on _TMP instead of DROP (preserves indexes for recovery)  
**Final status:** COMPLETED — both tables skipped (all rows already within retention year >= 2025)

---

## Timeline

| Time | Event | Detail |
|---|---|---|
| 05:13 | Deploy | Db2-LargeTableYearSplit.ps1 + Replay-CapturedDdl.ps1 signed and deployed |
| 07:59 | DROP _TMP tables | VISMA_TRANSER_TMP (103M rows) + D365_BUNTER_TMP (201M rows) dropped. Verified: 0 _TMP tables remain. |
| 08:01 | Pipeline triggered | PID 13272, submissionId 415f5de2fd6e, -RestoreFromPrd:$false |
| 08:02 | VISMA_TRANSER | 54,420,049 rows, all year >= 2025. **Skipped** — nothing to move. |
| 08:07 | D365_BUNTER | 54,203,066 rows, all year >= 2025. **Skipped** — nothing to move. |
| 08:15 | JOB_COMPLETED | Pipeline finished with exit 0, no errors |

---

## Result

Both tables were already split by the previous run (2026-03-24). The old rows (year < 2025) were in `_TMP` tables which we dropped before this run. The current tables only contain rows from 2025+, so the cutoff `YEAR >= 2025` matches all rows. No RenameAndReload was performed.

| Table | Rows | Kept | To TMP | Outcome |
|---|---|---|---|---|
| DBM.VISMA_TRANSER | 54,420,049 | 54,420,049 | 0 | Skipped |
| DBM.D365_BUNTER | 54,203,066 | 54,203,066 | 0 | Skipped |

## Errors

None.

## Fix-Deploy-Restart Cycles

None.

## Note

The RENAME INDEX refactoring (replacing DROP INDEX from _TMP with RENAME INDEX on _TMP) was deployed but not exercised in this run since no tables needed splitting. The new code path (Step 5b/7) will be validated when there are actual rows to archive (i.e., when records from before 2025 exist in the database, typically after a `RestoreFromPrd`).
