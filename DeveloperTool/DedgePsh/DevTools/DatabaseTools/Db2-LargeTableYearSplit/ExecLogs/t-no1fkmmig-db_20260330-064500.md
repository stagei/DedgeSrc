# Large Table Year Split — Autocur Run

| Field | Value |
|-------|-------|
| Server | t-no1fkmmig-db |
| Started (local) | 2026-03-30 06:45 UTC+1 (approx) |
| Project slot | large-table-split |
| Command | Db2-LargeTableYearSplit.ps1 |

## Parameters (actual server run)

- `-RestoreFromPrd:$true -ReuseLocalBackup:$true` (orchestrator / `running_command_*.json`)
- Config: `ArchiveTables.json` on server

## Log

- 06:44 — Deploy completed from dev machine (`_deploy.ps1`); Db2-Handler + app copied to t-no1fkmmig-db.
- 06:45 — **No new orchestrator submission** — slot `FKGEISTA_large-table-split` already **BUSY** (PID 9576).
  - Running command: `Db2-LargeTableYearSplit.ps1`
  - Arguments: `-RestoreFromPrd:$true -ReuseLocalBackup:$true`
  - Started: 2026-03-29 18:54:03 (elapsed ~12h50 at check time)
- **VISMA_TRANSER:** `RenameAndReload COMPLETE` at 2026-03-30 01:35:50 — Indexes 8/8, Grants 15/15, etc.
- **D365_BUNTER:** LOAD complete 56,182,323 rows at 02:26:51. **Step 7/7** in progress — replaying 24 indexes, 1 view, 22 grants (indexes created one-by-one; each can take ~30–60+ minutes on this volume).
- **Note:** During D365 pre-rename index phase, one system index rename failed with DB21034E (`SYSIBM.SQL211117200815200`); workflow continued with WARN — monitor final `Indexes: N/N OK` tally for D365.
- Fresh log copy: `Get-SplitJobStatus` → `%TEMP%\SplitJobStatus\central_20260329.log` (job spans midnight; tail after line ~18582 for D365 Step 7).
- 12:21 — Deploy: CommonModules unchanged (skip); `Db2-LargeTableYearSplit` folder synced (includes ExecLogs). Orchestrator still **BUSY** PID 9576, elapsed ~**17h27m**, same args.
- **D365 Step 7 (progress):** All **24** `Index OK` lines for `D365_BUNTER` recorded through **12:11:53** (last index `D365_BUNTER_I9`). Next logged step: `ALTER TABLE ... ADD PRIMARY KEY (...)` at **12:12:06** — PK / constraint phase after indexes. No second `=== RenameAndReload COMPLETE ===` for D365 and no `JOB_COMPLETED` in copied logs yet — job still running.
