-- KJØRES MOT  BASISPRO På SFKERP01 SLIK: K:\FKAVD\BASISHST>REXX FKSQL KASSTR_DEL BASISPRO LOG\KASSTR_DEL_2017B.LOG
-- KJØRES MOT BASISPRO på SFKERP01
-- SIST KJØRT 14 mars 2024 MEH
-- 
-- Ingen ting trenger å endres i denne jobben fra gang til gang !
--
 CONNECT TO BASISPRO USER srv_db2  USING Database1;
--
DELETE   FROM DBM.kass_transer A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 000000 and 150000);                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 150000 and 200000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 200000 and 250000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 250000 and 300000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 300000 and 350000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 350000 and 400000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 400000 and 450000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 450000 and 500000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 500000 and 550000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 550000 and 600000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 600000 and 650000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 650000 and 700000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 700000 and 750000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 750000 and 800000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 800000 and 850000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 850000 and 900000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 900000 and 910000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 910000 and 920000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 920000 and 930000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 930000 and 940000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 940000 and 950000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 960000 and 970000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 970000 and 980000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 980000 and 990000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 990000 and 999999);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 900000 and 950000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 900000 and 950000);
DELETE   FROM DBM.KASS_TRANSER A
WHERE EXISTS (SELECT 1 FROM DBM.KASSTR_TIL_HIST WHERE AVDNR = A.AVDNR AND BILAGNR = A.BILAGNR  and BILAGNR between 950000 and 999999);
