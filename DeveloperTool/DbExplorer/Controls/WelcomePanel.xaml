<UserControl x:Class="DbExplorer.Controls.WelcomePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             Background="{DynamicResource SystemControlBackgroundAltHighBrush}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="600">
            <!-- Welcome Header -->
            <TextBlock Text="DbExplorer" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,10"/>
            
            <TextBlock Text="Professional Database Manager" 
                       FontSize="16" 
                       Foreground="Gray" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"/>
            
            <!-- Supported database providers -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                <StackPanel Margin="0,0,24,0" HorizontalAlignment="Center">
                    <Image Source="pack://application:,,,/DbExplorer;component/Resources/Providers/db2.png" 
                           Width="64" Height="64" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="IBM DB2" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
                <StackPanel Margin="0,0,24,0" HorizontalAlignment="Center">
                    <Image Source="pack://application:,,,/DbExplorer;component/Resources/Providers/postgresql.png" 
                           Width="64" Height="64" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="PostgreSQL" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center">
                    <Image Source="pack://application:,,,/DbExplorer;component/Resources/Providers/sqlite.png" 
                           Width="64" Height="64" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="SQLite" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Quick Actions -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,40">
                <Button Content="➕ New Connection" 
                        Click="NewConnection_Click"
                        Padding="20,12" 
                        FontSize="16"
                        Margin="0,0,10,0"
                        MinWidth="200"/>
                
                <Button Content="📋 Manage Connections" 
                        Click="ManageConnections_Click"
                        Padding="20,12" 
                        FontSize="16"
                        MinWidth="200"/>
            </StackPanel>
            
            <!-- Recent Connections -->
            <Border BorderBrush="{DynamicResource SystemControlForegroundBaseMediumBrush}" 
                    BorderThickness="1" 
                    CornerRadius="4"
                    Padding="20"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
                
                <StackPanel>
                    <TextBlock Text="Recent Connections" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,15"/>
                    
                    <ListView x:Name="RecentConnectionsList" 
                              SelectionMode="Single"
                              MouseDoubleClick="RecentConnection_DoubleClick"
                              ContextMenuOpening="RecentConnection_ContextMenuOpening"
                              MaxHeight="400">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Icon -->
                                    <TextBlock Grid.Column="0" 
                                               Text="🗄️" 
                                               FontSize="24" 
                                               VerticalAlignment="Center"/>
                                    
                                    <!-- Connection Info -->
                                    <StackPanel Grid.Column="1" Margin="10,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding ProfileName}" 
                                                       FontWeight="SemiBold" 
                                                       FontSize="14"/>
                                            <Border Background="{DynamicResource SystemAccentColorLight2Brush}" 
                                                    CornerRadius="3" 
                                                    Padding="6,2" 
                                                    Margin="8,0,0,0"
                                                    VerticalAlignment="Center">
                                                <TextBlock Text="{Binding ProviderName}" 
                                                           FontSize="10" 
                                                           FontWeight="SemiBold"
                                                           Foreground="White"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock FontSize="12" 
                                                   Foreground="Gray">
                                            <Run Text="Last used:"/>
                                            <Run Text="{Binding LastUsedRelative}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding LastUsedDisplay}" 
                                                   FontSize="11" 
                                                   Foreground="Gray"/>
                                    </StackPanel>
                                    
                                    <!-- Use Count -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding UseCount}" 
                                                   FontSize="18" 
                                                   FontWeight="Bold" 
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="uses" 
                                                   FontSize="10" 
                                                   Foreground="Gray" 
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        
                        <!-- Context Menu -->
                        <ListView.ContextMenu>
                            <ContextMenu x:Name="ConnectionContextMenu">
                                <MenuItem Header="🔗 Connect" Click="ContextConnect_Click"/>
                                <MenuItem Header="✏️ Edit" Click="ContextEdit_Click"/>
                                <MenuItem Header="📋 Duplicate" Click="ContextCopy_Click"/>
                                <MenuItem Header="🧪 Test Connection" Click="ContextTest_Click"/>
                                <Separator/>
                                <MenuItem Header="🗑️ Remove from History" Click="ContextRemoveHistory_Click"/>
                                <MenuItem Header="🗑️ Delete Connection" Click="ContextDelete_Click" Foreground="Red"/>
                            </ContextMenu>
                        </ListView.ContextMenu>
                    </ListView>
                    
                    <TextBlock x:Name="NoConnectionsText" 
                               Text="No recent connections. Create a new connection to get started." 
                               Foreground="Gray" 
                               FontStyle="Italic" 
                               Margin="0,10,0,0"
                               Visibility="Collapsed"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            
            <!-- Version Info -->
            <TextBlock x:Name="VersionText" 
                       Text="Version" 
                       FontSize="11" 
                       Foreground="Gray" 
                       HorizontalAlignment="Center" 
                       Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>

