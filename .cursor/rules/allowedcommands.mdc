---
description: 
globs: 
---

# Your rule content
Allowed commands 
dotnet new sln, dotnet new classlib, dotnet new webapi, dotnet new blazorserver, dotnet new xunit, dotnet add reference, dotnet add package, dotnet sln add, dotnet build, dotnet test, dotnet run, dotnet restore, dotnet publish, dotnet clean, dotnet watch, dotnet format, dotnet list, dotnet pack, dotnet nuget, dotnet tool, mkdir, cd, dir, ls, cp, mv, rm, cat, echo, pwd, touch, git init, git add, git commit, git status, git log, git branch, git checkout, git merge, git pull, git push, git fetch, git clone, git remote, npm install, npm start, npm test, npm run, npm build, npm audit, npm update, dotnet ef migrations add, dotnet ef database update, dotnet ef database drop, dotnet ef migrations remove, dotnet ef dbcontext scaffold, dotnet ef dbcontext info, dotnet ef migrations script, dotnet dev-certs https --trust, dotnet user-secrets set, dotnet user-secrets list, dotnet user-secrets clear, dotnet user-secrets remove, dotnet tool install, dotnet tool uninstall, dotnet tool update, dotnet publish -c Release, docker build, docker push, docker run, docker compose up, docker compose down, az webapp deploy, az acr build, rm -rf, sudo, chmod, chown